# Backend Environment Variables
# Copiar este archivo a .env para desarrollo local
# En Render, configurar estas variables en el dashboard

# MongoDB Connection String
# Obtener de MongoDB Atlas
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/dbname?retryWrites=true&w=majority

# JWT Secret Key
# Generar usando: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=tu_clave_secreta_muy_segura_aqui

# Puerto del servidor (Render asigna automáticamente)
PORT=5000

# URL del Frontend (para configurar CORS)
# Desarrollo: http://localhost:5173
# Producción: https://tu-app.vercel.app
FRONTEND_URL=http://localhost:5173

# Environment
NODE_ENV=development
